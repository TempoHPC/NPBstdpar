   ============================================
   =      NAS PARALLEL BENCHMARKS 3.4         =
   =      OpenMP Versions                     =
   =      Fortran/C                           =
   ============================================

cd SP; make CLASS=A
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma sp_data.f
sed -e 's/= 0/= 0/' blk_par0.h > blk_par.h_wk
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma sp.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma initialize.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma exact_solution.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma exact_rhs.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma set_constants.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma adi.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma rhs.f
compute_rhs:
     23, Generating NVIDIA GPU code
         23,   ! blockidx%x threadidx%x auto-collapsed
             Loop parallelized across CUDA thread blocks, CUDA threads(128) collapse(3) ! blockidx%x threadidx%x
     23, Generating implicit copy(rho_i(:,:,:),vs(:,:,:),ws(:,:,:)) [if not already present]
         Generating implicit copyin(u(1:5,:,:,:)) [if not already present]
         Generating implicit copy(square(:,:,:),qs(:,:,:),us(:,:,:),speed(:,:,:)) [if not already present]
     48, Generating implicit copyout(rhs(1:5,0:nx2+1,0:ny2+1,0:nz2+1)) [if not already present]
         Generating implicit copyin(forcing(1:5,0:nx2+1,0:ny2+1,0:nz2+1)) [if not already present]
     50, Generating NVIDIA GPU code
         48,   ! blockidx%x threadidx%x auto-collapsed
             Loop parallelized across CUDA thread blocks, CUDA threads(128) collapse(4) ! blockidx%x threadidx%x
         50,   ! blockidx%x threadidx%x auto-collapsed
     59, Generating implicit copyout(rhs(1:5,2,1:ny2,1:nz2)) [if not already present]
         Generating implicit copyin(qs(0:nx2+1,1:ny2,1:nz2),us(0:nx2+1,1:ny2,1:nz2),u(1:5,:nx2+1,1:ny2,1:nz2),rho_i(0:nx2+1,1:ny2,1:nz2),vs(0:nx2+1,1:ny2,1:nz2),ws(0:nx2+1,1:ny2,1:nz2)) [if not already present]
         Generating implicit allocate(rhs(1:5,:nx2,1:ny2,1:nz2)) [if not already present]
         Generating implicit copyin(rhs(1:5,1:*,1:ny2,1:nz2)) [if not already present]
         Generating implicit copyin(square(0:nx2+1,1:ny2,1:nz2)) [if not already present]
     61, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
         61, Loop parallelized across CUDA thread blocks, CUDA threads(32) ! blockidx%x threadidx%x
    117, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
        117, Loop parallelized across CUDA thread blocks, CUDA threads(32) ! blockidx%x threadidx%x
    124, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
        124, Loop parallelized across CUDA thread blocks, CUDA threads(32) ! blockidx%x threadidx%x
    131, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
        130, Loop parallelized across CUDA thread blocks, CUDA threads(32) collapse(2) ! blockidx%x threadidx%x
        131,   ! blockidx%x threadidx%x auto-collapsed
    140, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
        140, Loop parallelized across CUDA thread blocks, CUDA threads(32) ! blockidx%x threadidx%x
    147, Generating NVIDIA GPU code
         59, Loop parallelized across CUDA thread blocks, CUDA threads(4) collapse(2) blockidx%y threadidx%y
               ! blockidx%y threadidx%y auto-collapsed
        147, Loop parallelized across CUDA thread blocks, CUDA threads(32) ! blockidx%x threadidx%x
    159, Generating NVIDIA GPU code
        159, Loop parallelized across CUDA thread blocks collapse(2) ! blockidx%x
               ! blockidx%x auto-collapsed
        161, Loop parallelized across CUDA threads(128) ! threadidx%x
        214, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        215,   ! threadidx%x auto-collapsed
        223, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        224,   ! threadidx%x auto-collapsed
        232, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        233,   ! threadidx%x auto-collapsed
        241, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        242,   ! threadidx%x auto-collapsed
        250, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        251,   ! threadidx%x auto-collapsed
    159, Generating implicit copyin(ws(1:nx2,0:ny2+1,1:nz2),vs(1:nx2,0:ny2+1,1:nz2),u(1:5,1:nx2,-1:ny2+2,1:nz2),us(1:nx2,0:ny2+1,1:nz2),rho_i(1:nx2,0:ny2+1,1:nz2)) [if not already present]
         Generating implicit copy(rhs(1:5,1:nx2,1:ny2,1:nz2)) [if not already present]
         Generating implicit copyin(square(1:nx2,0:ny2+1,1:nz2),qs(1:nx2,0:ny2+1,1:nz2)) [if not already present]
    266, Generating NVIDIA GPU code
        266, Loop parallelized across CUDA thread blocks collapse(2) ! blockidx%x
               ! blockidx%x auto-collapsed
        268, Loop parallelized across CUDA threads(128) ! threadidx%x
        321, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        322,   ! threadidx%x auto-collapsed
        330, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        331,   ! threadidx%x auto-collapsed
        339, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        340,   ! threadidx%x auto-collapsed
        348, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        349,   ! threadidx%x auto-collapsed
        357, Loop parallelized across CUDA threads(128) collapse(2) ! threadidx%x
        358,   ! threadidx%x auto-collapsed
    266, Generating implicit copyin(grid_points(:),qs(:,:,:),us(:,:,:),rho_i(:,:,:),ws(:,:,:),vs(:,:,:),u(1:5,:,:,:)) [if not already present]
         Generating implicit copy(rhs(1:5,:,:,:)) [if not already present]
         Generating implicit copyin(square(:,:,:)) [if not already present]
    369, Generating implicit copy(rhs(1:5,1:nx2,1:ny2,1:nz2)) [if not already present]
    371, Generating NVIDIA GPU code
        369,   ! blockidx%x threadidx%x auto-collapsed
             Loop parallelized across CUDA thread blocks, CUDA threads(128) collapse(4) ! blockidx%x threadidx%x
        371,   ! blockidx%x threadidx%x auto-collapsed
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma work_lhs.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma x_solve.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma ninvr.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma y_solve.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma pinvr.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma z_solve.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma tzetar.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma add.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma txinvr.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma error.f
nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma verify.f
cd ../common; nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma print_results.f
cd ../common; nvfortran -c  -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma timers.f
cd ../common; nvc -c  -O3  -o wtime.o ../common/wtime.c
nvfortran -O3 -stdpar=gpu -Minfo=stdpar -gpu=cc70,fastmath,loadcache:L1,unroll,fma -o ../bin/sp.A.x sp.o sp_data.o initialize.o exact_solution.o exact_rhs.o set_constants.o adi.o rhs.o work_lhs.o x_solve.o ninvr.o y_solve.o pinvr.o z_solve.o tzetar.o add.o txinvr.o error.o verify.o ../common/print_results.o ../common/timers.o ../common/wtime.o
make: *** No rule to make target '2'.  Stop.
